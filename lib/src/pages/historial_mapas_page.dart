import 'package:flutter/material.dart';

class HistorialMapasPage extends StatelessWidget {
  const HistorialMapasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Mapas historial"),
    );
  }
}