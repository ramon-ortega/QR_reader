import 'package:flutter/material.dart';

class HistorialPaginasPage extends StatelessWidget {
  const HistorialPaginasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Paginas historial"),
    );
  }
}